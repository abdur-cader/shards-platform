import React from 'react'

const Herotemp = () => {
  return (
    <div className="relative w-full h-[45vh] overflow-hidden bg-white">
      {/* Empty div with same dimensions as original */}
      <div className="absolute inset-0"></div>
    </div>
  );
};

export default Herotemp;